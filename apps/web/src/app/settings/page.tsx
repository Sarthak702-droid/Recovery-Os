export default function Settings(){return <main className="page"><header className="page-head"><div><div className="eyebrow">Workspace settings</div><h1 className="page-title">Settings</h1><p className="page-subtitle">Merchant identity and sensitive provider credentials are configured server-side through environment-managed deployment settings.</p></div></header><section className="card"><div className="card-body"><div className="notice">Security boundary: API keys, webhook secrets, SMTP passwords, database credentials, and model infrastructure details never enter the browser bundle.</div></div></section></main>}
