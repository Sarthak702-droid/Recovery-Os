'use client';
import {useQuery} from '@tanstack/react-query'; import {recoverosApi} from '@/lib/api/recoveros'; import {queryKeys} from '@/lib/query/keys'; import {EmptyState,ErrorState,LoadingState} from '@/components/shared/states'; import {StatusBadge} from '@/components/shared/ui';
export default function Audit(){const q=useQuery({queryKey:queryKeys.audit,queryFn:recoverosApi.audit});if(q.isLoading)return <main className="page"><LoadingState/></main>;if(q.isError||!q.data)return <main className="page"><ErrorState title="Audit records are unavailable"/></main>;return <main className="page"><header className="page-head"><div><div className="eyebrow">Compliance history</div><h1 className="page-title">Audit trail</h1><p className="page-subtitle">Read-only append-only records from the RecoverOS audit ledger.</p></div></header><section className="card">{!q.data.length?<EmptyState title="No audit events have been recorded" description="Signed provider events and recovery workflow transitions will create immutable audit records."/>:<div className="table-wrap"><table className="data-table"><thead><tr><th>Time</th><th>Case</th><th>Actor</th><th>Event</th><th>Result</th><th>Trace ID</th></tr></thead><tbody>{q.data.map(row=><tr key={row.id}><td>{row.created_at?new Date(row.created_at).toLocaleString():'—'}</td><td>{row.case_id||'—'}</td><td>{row.actor}</td><td>{row.event_type.replaceAll('_',' ')}</td><td><StatusBadge value={row.after_state||row.before_state||'RECORDED'}/></td><td>{row.trace_id||'—'}</td></tr>)}</tbody></table></div>}</section></main>}
